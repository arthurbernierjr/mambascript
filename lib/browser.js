// Generated by KofuScript 0.1.0-beta1 
var present = console.log; 
// Generated by KofuScript 0.1.0-beta1 
var KofuScript, runScripts;
module.exports = KofuScript = require('./module');
KofuScript['eval'] = function (code, options) {
  if (null == options)
    options = {};
  if (null != options.bare)
    options.bare;
  else
    options.bare = true;
  if (null != options.optimise)
    options.optimise;
  else
    options.optimise = true;
  return eval(KofuScript.cs2js(code, options));
};
KofuScript.run = function (code, options) {
  if (null == options)
    options = {};
  options.bare = true;
  if (null != options.optimise)
    options.optimise;
  else
    options.optimise = true;
  return Function(KofuScript.cs2js(code, options))();
};
KofuScript.load = function (url, callback) {
  var xhr;
  xhr = window.ActiveXObject ? new window.ActiveXObject('Microsoft.XMLHTTP') : new XMLHttpRequest;
  xhr.open('GET', url, true);
  if ('overrideMimeType' in xhr)
    xhr.overrideMimeType('text/plain');
  xhr.onreadystatechange = function () {
    if (!(xhr.readyState === xhr.DONE))
      return;
    if (xhr.status === 0 || xhr.status === 200) {
      KofuScript.run(xhr.responseText);
    } else {
      throw new Error('Greetings from the KofuScript.load function in the browser.coffer file  Could not load ' + url);
    }
    if (callback)
      return callback();
  };
  return xhr.send(null);
};
runScripts = function () {
  var coffees, execute, index, s, scripts;
  scripts = document.getElementsByTagName('script');
  coffees = function (accum$) {
    for (var i$ = 0, length$ = scripts.length; i$ < length$; ++i$) {
      s = scripts[i$];
      if (!(s.type === 'text/coffeescript'))
        continue;
      accum$.push(s);
    }
    return accum$;
  }.call(this, []);
  index = 0;
  (execute = function () {
    var script;
    if (!(script = coffees[index++]))
      return;
    if (script.src) {
      return KofuScript.load(script.src, execute);
    } else {
      KofuScript.run(script.innerHTML);
      return execute();
    }
  })();
  return null;
};
if ('undefined' !== typeof addEventListener && null != addEventListener) {
  addEventListener('DOMContentLoaded', runScripts, false);
} else if ('undefined' !== typeof attachEvent && null != attachEvent) {
  attachEvent('onload', runScripts);
}
